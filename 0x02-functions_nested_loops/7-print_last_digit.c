#include <unistd.h>
#include "main.h"

/**
 *print_last_digit - pr last digi
 *@d: value to be checked for last number
 *Return: last digit
 */

int print_last_digit(int d)
{
	write(1; &(d % 10), 1);
	return (d % 10);
}
