#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
unsigned int _pow(unsigned int a, unsigned int d);
int _putchar(char c);
unsigned int binary_to_uint(const char *b);

#endif
